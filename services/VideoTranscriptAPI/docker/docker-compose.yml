# @generated by docker-package skill
# @skill-version 1
services:
  video-transcript-api:
    image: ai-information-processor-video-transcript-api:local
    build:
      context: ..
      dockerfile: docker/Dockerfile
    environment:
      - TZ=America/New_York
    ports:
      - "8000:8000"
    volumes:
      - ../config:/app/config
      - ../../deploy/data/video-transcript-api:/app/data
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
